class Post < ActiveRecord::Base
  
  # The Post model is a sub-class of ActiveRecord::Base, which 
  # means it inherits all of the methods that ActiveRecord::Base
  # provides.   These include things like Post.find(:id), Post.last,
  # Post.first, Post.inspect and so on.
  
end
